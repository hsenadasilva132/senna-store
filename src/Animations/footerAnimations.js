import gsap from 'gsap';
import { ScrollTrigger } from 'gsap/ScrollTrigger';
//import { SplitText } from 'gsap/SplitText';

gsap.registerPlugin(ScrollTrigger);

export const footerAnimation = () => {
 /*  gsap.from(".footer-divisor", {
        scaleX: 0,
        duration: 1.2,
        ease: "power3.out",

        scrollTrigger: {
            trigger: ".footer",
            start: "top 85%"
        }
    }); */
   
}